/**
 * ASK Kit - AppSolution Social Kit
 * A comprehensive npm package for rapid Telegram mini-app development
 */

// Export the dump utility
export { default as dump } from './utils/dump'

// Version - automatically updated from package.json
export const VERSION = '0.0.3'

// Default export for convenience
export default {
  VERSION,
}
